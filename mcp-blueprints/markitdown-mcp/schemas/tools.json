{
  "tools": [
    {
      "name": "convert_local_file",
      "description": "Convert a local file (PDF, DOCX, XLSX, etc.) to Markdown.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "path": {
            "type": "string",
            "maxLength": 1000,
            "description": "Absolute path to the local file to convert"
          }
        },
        "required": ["path"]
      }
    },
    {
      "name": "convert_remote_url",
      "description": "Fetch a remote document or webpage via HTTP/HTTPS and convert to Markdown.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "url": {
            "type": "string",
            "maxLength": 2048,
            "pattern": "^https?://",
            "description": "Remote URL to fetch and convert"
          }
        },
        "required": ["url"]
      }
    }
  ]
}
